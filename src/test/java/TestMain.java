import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.support.ui.Wait;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.time.Duration;

public class TestMain {
    public static void main(String[] args) {
//        System.setProperty("webdriver.chrome.driver", "src/test/resources/chromedriver.exe");
//        final var options= new ChromeOptions();
//        options.addArguments("--remote-allow-origins=*");
//        final var driver = new ChromeDriver(options);
//        driver.manage().window().maximize();
//
//        driver.get("https://www.selenium.dev/selenium/web/web-form.html");
//
//        final var textBox = driver.findElement(By.name("my-text"));
//        final var submitButton = driver.findElement(By.cssSelector("button[type='submit']"));
//
//        textBox.sendKeys("Humza");
//        System.out.println(textBox.getDomProperty("value"));
//        submitButton.click();
//
//        final var endMessage = driver.findElement(By.cssSelector("display-6"));
//
//        Wait<WebDriver> wait = new WebDriverWait(driver, Duration.ofSeconds(2));
//        wait.until(d -> driver.getCurrentUrl());
//        driver.close();

    }
}

